import React from 'react';

/**
 * Neutralized: Replaced by the unified, minimal production CustomCursor.
 * Kept as a safe no-op component to avoid dangling import errors.
 */
export const AmbientCursorLight: React.FC = () => null;
export default AmbientCursorLight;

